# perception — Phase 3–5: Perception layer sub-package
# SCHEMA_VERSION: 1
#
# Modules:
#   frame_diff.py  — frame differencing, dirty region detection, change classification
#   uia.py         — Windows UI Automation native element extractor
#   ocr.py         — PaddleOCR text extractor (region-based only)
#   identity.py    — perceptual hash for cross-frame element identity
